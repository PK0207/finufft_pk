[build-system]
requires = ["scikit-build-core", "nanobind"]
build-backend = "scikit_build_core.build"

[project]
name = "finufft-pk"
version = "0.1.0"
description = "A package to calculate the bandpowers of an FFT field faster and more accurately. Uses FINUFFT to transform a non-uniform set of points to a uniform grid and perform a FFT (uses FFTW under the hood)."
readme = "README.md"
requires-python = ">=3.11.11"
dependencies = ["finufft", "numpy"]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.scikit-build]
wheel.packages = ["cpp_testing", "src/finufft_pk"]

[tool.uv]
cache-keys = [{ file = "pyproject.toml" }, { file = "CMakeLists.txt" }, { file = "cpp_testing/**/*.cpp" }, { file = "src/finufft_pk/**/*.cpp" }]
